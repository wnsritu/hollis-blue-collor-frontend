import { ENDPOINTS } from "@/constants/endpoints";

export const PROVIDER_ENDPOINTS = ENDPOINTS.provider;
export const MARKETPLACE_PROVIDER_ENDPOINTS = ENDPOINTS.marketplaceProvider;

export default PROVIDER_ENDPOINTS;
