import { ENDPOINTS } from "@/constants/endpoints";

export const AUTH_ENDPOINTS = ENDPOINTS.auth;
export const FORGOT_PASSWORD_LEGACY_ENDPOINTS = ENDPOINTS.forgotPasswordLegacy;

export default AUTH_ENDPOINTS;
