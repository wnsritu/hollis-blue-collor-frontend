import { ENDPOINTS } from "@/constants/endpoints";

export const ORDER_ENDPOINTS = ENDPOINTS.order;
export const PROJECT_ENDPOINTS = ENDPOINTS.project;
export const PROPOSAL_ENDPOINTS = ENDPOINTS.proposal;

export default ORDER_ENDPOINTS;
